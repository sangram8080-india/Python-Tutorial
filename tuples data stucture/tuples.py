#tuples data structure 
#tuples can store any data types 
#most importents tuples are immutable once tuple is created you can't update 

#ex-
tuples  = ('one','Two','three','one')

#tuples=tuples.count('one')
#tuples = tuples[2]
#tuples = len(tuples)

tuples = tuples[:2]  //slicing 

print(tuples)
